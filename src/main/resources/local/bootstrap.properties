# Configure the app name in the earliest phase of service configuration
# Used by Config client to fetch the relevant configuration from Config server
spring.application.name=test-spring-dataflow-server

# Use spring active profile to indicate the correspondent configuration from Config server
spring.profiles.active=local